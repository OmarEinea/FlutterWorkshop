import 'package:flutter/material.dart';

main() {
  runApp(
    MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: Text('Flutter Workshop')
        ),
        body: Center(
          child: Text('Welcome'),
        ),
      )
    )
  );
}